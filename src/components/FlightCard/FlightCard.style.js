import styled from "styled-components";

export const FlightCardWrapper = styled.div`
	margin: 3rem 2rem;
    background: var(--Grey);
`;

export const Icon = styled.div`
	width: 5rem;
`;

export const FlightCardSquare = styled.div`
    background: var(--White);
`
